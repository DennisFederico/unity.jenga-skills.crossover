namespace model {
    /// <summary>
    /// enum that represents the mastery level of a specific domain skill.
    /// </summary>
    public enum SkillMasteryLevel {
        Glass,
        Wood,
        Stone
    }
}